import math
ang = float(input("ANGULO: "))
seno = math.sin(math.radians(ang))
cos = math.cos(math.radians(ang))
tang = math.tan(math.radians(ang))
print(f"seno: {seno:.2f} coseno: {cos:.2f} tangente: {tang:.2f}")
